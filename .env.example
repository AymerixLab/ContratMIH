# Connection string consumed by the API server
DATABASE_URL=postgresql://mih:mih_password@db:5432/mih_staging
# Port used by the API server
PORT=8080
# Allow the dev frontend to hit the API (optional)
CORS_ORIGIN=http://localhost:3000
# Base URL for the frontend to reach the API (override in dev when ports differ)
VITE_API_BASE_URL=http://localhost:8080
